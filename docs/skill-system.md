# Skill System

Skills are markdown files stored under `workspace/skills/`.

Each skill should answer:

- What problem it solves
- When to use it
- What inputs it expects
- What procedure it follows
- What output it should produce

The runtime currently loads skill metadata and exposes list/read/create
operations. Future milestones will add retrieval, execution-aware prompting, and
automatic skill extraction from completed conversations.
